module.exports = {
    name: "error",
    execute(error, message) {
        switch (error) {
            case value:
                
                break;
        
            default:
                break;
        }
    }
}