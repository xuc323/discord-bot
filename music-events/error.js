module.exports = {
    name: "error",
    execute(error, message) {
        console.log("ERROR: ", error);
        // switch (error) {
        //     case value:

        //         break;

        //     default:
        //         break;
        // }
    }
}