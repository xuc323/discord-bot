module.exports = {
    name: "error",
    execute(error, message) {
        console.log(error);
        // switch (error) {
        //     case value:

        //         break;

        //     default:
        //         break;
        // }
    }
}